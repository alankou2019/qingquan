# A电脑交接：薪酬二级页面统一返回入口

## 本次调整

1. 薪酬管理一级页面仍为 `salary/index`。
2. Salary 模块下 19 个非首页页面，在顶部页签栏右侧统一增加“返回薪酬首页”。
3. 工资、工资条、授权、员工同步、报表、日志、提成等页面均已覆盖。
4. 页面内容区原有的同名返回按钮已移除，避免重复。
5. “返回工资表核算”“返回归档记录”“返回”等当前业务流程按钮继续保留。

## 验证情况

- 静态检查：19 个非首页模板全部且仅有一个“返回薪酬首页”入口。
- 页面抽测：工资项目、工资核算、归档、工资条、授权、员工同步、报表、日志和提成主要页面均返回 HTTP 200，无模板或 PHP 错误。
- 浏览器实测：`salary/commissionestimate` 顶部按钮位置正常，点击后进入 `salary/index`。

## A电脑注意

1. 本提交只调整薪酬模块前端模板，没有修改营运后台、数据库和生产配置。
2. A 电脑继续开发前先同步本提交，不要手工复制覆盖项目目录。
3. 如需调整 Salary 模块页签，请保留右侧统一返回入口。
4. 仍需在开发/测试环境人工确认后才能考虑生产发布。

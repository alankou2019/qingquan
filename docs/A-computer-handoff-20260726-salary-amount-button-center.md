# A 电脑交接：设置金额按钮居中

日期：2026-07-26

## 本次修改

- 初始工资表表头中的“设置金额”按钮改为水平居中。
- 工资核算表表头中的“设置金额”按钮改为水平居中。
- 按钮与上方工资项目名称保持同一列居中对齐。
- 仅调整页面样式，不修改金额设置逻辑、工资项目和工资数据。

## 修改文件

- `app/design/frontend/default/Salary/salary/project.volt`
- `app/design/frontend/default/Salary/salary/payroll.volt`

## 测试结果

- 两个页面均正常访问。
- 浏览器实测初始工资表按钮中心与表头中心偏差为 `0px`。
- 浏览器实测工资核算表按钮中心与表头中心偏差为 `0px`。
- `git diff --check` 通过。

## 同步注意

- 营运后台代码未修改。
- 不涉及数据库变更。
- 不要直接部署生产，先在测试环境完成人工测试。
